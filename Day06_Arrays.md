# Day 06: 배열 & 문자열 — "사물함에 번호를 붙이자!" 🗄️

## 📚 학습 목표
- 1차원 배열의 개념, 선언, 초기화, 순회 이해
- 문자열이 char 배열임을 이해하고 문자열 함수 활용
- 배열을 함수에 전달하는 방법 체득
- 2차원 배열의 기초 개념 이해

## ⏰ 시간 배분 (3시간)
| 시간 | 내용 | 이론 | 실습 |
|------|------|------|------|
| 50분 | **1교시**: 1차원 배열 — 번호 붙은 사물함 | 20분 | 30분 |
| 10분 | 휴식 | - | - |
| 50분 | **2교시**: 문자열 — char 배열 + 마법 문자 | 20분 | 30분 |
| 10분 | 휴식 | - | - |
| 50분 | **3교시**: 배열과 함수 & 종합 실습 | 15분 | 35분 |
| 10분 | Q&A 및 정리 | - | - |

> **강사 노트**: 배열 = "사물함", 인덱스 = "사물함 번호"라는 비유를 일관되게 사용합니다.

---

## 🎯 1교시: 1차원 배열 — "번호 붙은 사물함" (50분)

### 📖 이론 (20분)

#### 1.1 배열이란?

**배열 = 번호가 붙은 사물함** 🗄️

```
사물함  [0]  [1]  [2]  [3]  [4]
        ┌────┬────┬────┬────┬────┐
값      │ 85 │ 92 │ 78 │ 96 │ 88 │
        └────┴────┴────┴────┴────┘
         ↑ 0번부터 시작!
```

- 같은 타입의 데이터를 여러 개 저장
- **인덱스는 0부터 시작!** (가장 많이 하는 실수!)

#### 1.2 배열 선언과 초기화

```c
// 방법 1: 선언만 (쓰레기 값이 들어있음)
int scores[5];

// 방법 2: 선언 + 초기화
int scores[5] = {85, 92, 78, 96, 88};

// 방법 3: 일부만 초기화 (나머지는 0)
int scores[5] = {85, 92};  // → {85, 92, 0, 0, 0}

// 방법 4: 모두 0으로 초기화
int scores[5] = {0};  // → {0, 0, 0, 0, 0}

// 방법 5: 크기 생략 (개수만큼 자동 설정)
int scores[] = {85, 92, 78};  // 크기 3 자동!
```

#### 1.3 배열 접근

```c
int scores[5] = {85, 92, 78, 96, 88};

scores[0];  // 85  (첫 번째)
scores[4];  // 88  (마지막)
scores[2] = 100;  // 세 번째 값 변경!

// ❌ 주의! 범위 밖 접근 금지!
// scores[5];  // 위험! 쓰레기 값 or 크래시!
```

#### 1.4 배열 + 반복문 = 최고의 궁합!

```c
int scores[5] = {85, 92, 78, 96, 88};

// 전체 출력
for (int i = 0; i < 5; i++) {
    printf("scores[%d] = %d\n", i, scores[i]);
}
```

---

### 💻 실습 (30분)

#### 💻 실습 6-1: 5명 점수 합계 & 평균 (10분)

```c
#include <stdio.h>

int main() {
    int scores[5];
    int sum = 0;
    
    printf("=== 📊 성적 계산기 ===\n");
    for (int i = 0; i < 5; i++) {
        printf("%d번 학생 점수: ", i + 1);
        scanf("%d", &scores[i]);
        sum += scores[i];
    }
    
    printf("\n--- 결과 ---\n");
    printf("합계: %d\n", sum);
    printf("평균: %.1f\n", sum / 5.0);
    
    return 0;
}
```

---

#### 💻 실습 6-2: 최대값 & 최소값 찾기 (10분)

```c
#include <stdio.h>

int main() {
    int arr[5] = {34, 78, 12, 95, 56};
    int max = arr[0];  // 첫 번째를 일단 최대로!
    int min = arr[0];  // 첫 번째를 일단 최소로!
    
    for (int i = 1; i < 5; i++) {
        if (arr[i] > max) max = arr[i];
        if (arr[i] < min) min = arr[i];
    }
    
    printf("배열: ");
    for (int i = 0; i < 5; i++) printf("%d ", arr[i]);
    printf("\n최대값: %d\n", max);
    printf("최소값: %d\n", min);
    
    return 0;
}
```

**실행 결과:**
```
배열: 34 78 12 95 56
최대값: 95
최소값: 12
```

---

#### 💻 실습 6-3: 배열 뒤집기 (10분)

```c
#include <stdio.h>

int main() {
    int arr[5] = {1, 2, 3, 4, 5};
    
    printf("원래 배열: ");
    for (int i = 0; i < 5; i++) printf("%d ", arr[i]);
    
    // 뒤집기: 양쪽 끝에서 교환!
    for (int i = 0; i < 5 / 2; i++) {
        int temp = arr[i];
        arr[i] = arr[4 - i];
        arr[4 - i] = temp;
    }
    
    printf("\n뒤집은 배열: ");
    for (int i = 0; i < 5; i++) printf("%d ", arr[i]);
    printf("\n");
    
    return 0;
}
```

**실행 결과:**
```
원래 배열: 1 2 3 4 5
뒤집은 배열: 5 4 3 2 1
```

---

## 🎯 2교시: 문자열 — "char 배열 + 마법 문자" (50분)

### 📖 이론 (20분)

#### 2.1 C의 문자열 = char 배열 + '\0'

```
"Hello" 가 메모리에 저장되는 모습:

[0]  [1]  [2]  [3]  [4]  [5]
 H    e    l    l    o   \0   ← 끝을 알려주는 마법 문자!
```

- `\0` (널 문자) = "여기가 문자열의 끝이야!"
- 그래서 `"Hello"`는 5글자지만 **6칸** 필요!

```c
// 문자열 선언 방법
char name1[10] = "Hello";        // 크기 지정
char name2[] = "Hello";          // 크기 자동 (6)
char name3[10] = {'H','e','l','l','o','\0'};  // 한 글자씩
```

#### 2.2 문자열 입출력

```c
char name[20];

// 입력 (공백 없는 단어)
scanf("%s", name);    // name 앞에 & 안 붙임! (배열이니까)

// 입력 (공백 포함 문장) — 추천!
fgets(name, 20, stdin);

// 출력
printf("%s\n", name);
```

> **⚠️ scanf vs fgets**
> - `scanf("%s")`: "Hello World" → "Hello"만 읽음 (공백에서 끊김!)
> - `fgets()`: "Hello World" → 전부 읽음 ✅ (안전함!)

#### 2.3 문자열 함수 (string.h)

```c
#include <string.h>

strlen(s)         // 길이 (문자 수)
strcpy(dst, src)  // 복사 (src → dst)
strcmp(s1, s2)     // 비교 (같으면 0)
strcat(dst, src)  // 이어 붙이기 (dst 뒤에 src)
```

```c
char a[20] = "Hello";
char b[20] = "World";

strlen(a);           // 5
strcpy(a, "Hi");     // a = "Hi"
strcmp("abc", "abc"); // 0 (같다!)
strcat(a, b);        // a = "HiWorld"
```

---

### 💻 실습 (30분)

#### 💻 실습 6-4: 이름 입력 → 인사 (5분)

```c
#include <stdio.h>
#include <string.h>

int main() {
    char name[30];
    
    printf("이름을 입력하세요: ");
    scanf("%s", name);
    
    printf("👋 %s님, 반갑습니다!\n", name);
    printf("이름 글자 수: %d\n", (int)strlen(name));
    
    return 0;
}
```

---

#### 💻 실습 6-5: 문자열 길이 직접 세기 (10분)

**목표**: `strlen` 없이 직접 구현! (문자열 원리 이해)

```c
#include <stdio.h>

int my_strlen(char str[]) {
    int count = 0;
    while (str[count] != '\0') {
        count++;
    }
    return count;
}

int main() {
    char word[50];
    
    printf("단어를 입력하세요: ");
    scanf("%s", word);
    
    printf("직접 센 길이: %d\n", my_strlen(word));
    
    return 0;
}
```

---

#### 💻 실습 6-6: 대문자 ↔ 소문자 변환 (15분)

```c
#include <stdio.h>
#include <string.h>

int main() {
    char word[50];
    
    printf("영단어를 입력하세요: ");
    scanf("%s", word);
    
    printf("원래 단어: %s\n", word);
    
    // 대소문자 변환
    for (int i = 0; i < (int)strlen(word); i++) {
        if (word[i] >= 'a' && word[i] <= 'z') {
            word[i] = word[i] - 32;  // 소문자 → 대문자
        } else if (word[i] >= 'A' && word[i] <= 'Z') {
            word[i] = word[i] + 32;  // 대문자 → 소문자
        }
    }
    
    printf("변환 결과: %s\n", word);
    
    return 0;
}
```

**실행 결과:**
```
영단어를 입력하세요: Hello
원래 단어: Hello
변환 결과: hELLO
```

> **핵심 포인트**: ASCII 코드에서 `'a' - 'A' = 32`!

---

## 🎯 3교시: 배열과 함수 & 종합 실습 (50분)

### 📖 이론 (15분)

#### 3.1 배열을 함수에 전달하기

배열은 함수에 **주소로 전달**됩니다! (원본이 바뀔 수 있음!)

```c
// 배열을 매개변수로 받기
void print_array(int arr[], int size) {
    for (int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}

int sum_array(int arr[], int size) {
    int total = 0;
    for (int i = 0; i < size; i++) {
        total += arr[i];
    }
    return total;
}

int main() {
    int scores[] = {85, 92, 78, 96, 88};
    
    print_array(scores, 5);  // 85 92 78 96 88
    printf("합계: %d\n", sum_array(scores, 5));  // 439
    
    return 0;
}
```

> **왜 `size`도 같이 전달할까?**
> → 함수 안에서는 배열의 크기를 알 수 없기 때문!

#### 3.2 2차원 배열 맛보기

```
행렬 (2x3):
    [0][0] [0][1] [0][2]
    ┌──────┬──────┬──────┐
    │  1   │  2   │  3   │  → 0행
    ├──────┼──────┼──────┤
    │  4   │  5   │  6   │  → 1행
    └──────┴──────┴──────┘
```

```c
int matrix[2][3] = {
    {1, 2, 3},
    {4, 5, 6}
};

// 접근
matrix[0][1];  // 2 (0행 1열)
matrix[1][2];  // 6 (1행 2열)
```

---

### 💻 실습 (35분)

#### 💻 실습 6-7: 성적 관리 프로그램 — 배열 + 함수 (15분)

```c
#include <stdio.h>

#define MAX 5

void input_scores(int arr[], int size) {
    for (int i = 0; i < size; i++) {
        printf("%d번 학생 점수: ", i + 1);
        scanf("%d", &arr[i]);
    }
}

int find_max(int arr[], int size) {
    int max = arr[0];
    for (int i = 1; i < size; i++) {
        if (arr[i] > max) max = arr[i];
    }
    return max;
}

double calc_avg(int arr[], int size) {
    int sum = 0;
    for (int i = 0; i < size; i++) sum += arr[i];
    return (double)sum / size;
}

int main() {
    int scores[MAX];
    
    printf("=== 📊 성적 관리 시스템 ===\n");
    input_scores(scores, MAX);
    
    printf("\n--- 📋 결과 ---\n");
    printf("최고 점수: %d\n", find_max(scores, MAX));
    printf("평균 점수: %.1f\n", calc_avg(scores, MAX));
    
    return 0;
}
```

---

#### 💻 종합 실습 6-8: 🎮 단어 퀴즈 게임 (20분)

```c
#include <stdio.h>
#include <string.h>

int main() {
    // 퀴즈 데이터
    char questions[][50] = {
        "사과를 영어로?",
        "고양이를 영어로?",
        "학교를 영어로?",
        "컴퓨터를 영어로?",
        "음악을 영어로?"
    };
    char answers[][20] = {
        "apple", "cat", "school", "computer", "music"
    };
    
    int score = 0;
    int total = 5;
    char input[20];
    
    printf("🎮 === 영단어 퀴즈! === 🎮\n\n");
    
    for (int i = 0; i < total; i++) {
        printf("Q%d. %s\n", i + 1, questions[i]);
        printf("답: ");
        scanf("%s", input);
        
        if (strcmp(input, answers[i]) == 0) {
            printf("✅ 정답입니다!\n\n");
            score++;
        } else {
            printf("❌ 오답! 정답은 '%s'\n\n", answers[i]);
        }
    }
    
    printf("===== 결과 =====\n");
    printf("점수: %d / %d", score, total);
    if (score == total) printf(" 🏆 만점!");
    else if (score >= 3) printf(" 👍 좋아요!");
    else printf(" 💪 분발하세요!");
    printf("\n");
    
    return 0;
}
```

---

## 📝 핵심 정리

| 개념 | 설명 | 예시 |
|------|------|------|
| **배열 선언** | 같은 타입 여러 개 저장 | `int arr[5];` |
| **인덱스** | 0부터 시작! | `arr[0]` ~ `arr[4]` |
| **초기화** | 선언 시 값 지정 | `int arr[] = {1,2,3};` |
| **문자열** | char 배열 + `\0` | `char s[] = "Hi";` |
| **strlen** | 문자열 길이 | `strlen("Hi")` → 2 |
| **strcpy** | 문자열 복사 | `strcpy(a, b)` |
| **strcmp** | 문자열 비교 | `strcmp(a, b) == 0` |
| **배열→함수** | 크기도 같이 전달 | `func(arr, size)` |

---

## 📝 과제

### 과제 1: 성적 관리 배열
5명의 국어, 영어, 수학 점수를 2차원 배열에 저장하고, 각 학생의 총점과 평균을 구하세요.

### 과제 2: 단어 뒤집기
사용자가 입력한 단어를 뒤집어 출력하세요.
- 예: `"hello"` → `"olleh"`

### 과제 3: 간단한 암호화
입력한 문자열의 각 문자에 +1을 해서 암호화하세요.
- 예: `"abc"` → `"bcd"`, `"hello"` → `"ifmmp"`
