# Day 07: 포인터 & 구조체 — "주소와 프로필 카드" 📮

## 📚 학습 목표
- 포인터의 개념(주소 저장 변수) 이해
- `&`(주소), `*`(역참조) 연산자 활용
- 포인터로 함수에서 값을 변경하기 (Call by Reference)
- 구조체로 관련 데이터를 하나로 묶기
- C 언어 핵심 내용 총정리

## ⏰ 시간 배분 (3시간)
| 시간 | 내용 | 이론 | 실습 |
|------|------|------|------|
| 50분 | **1교시**: 포인터 기초 — 집 주소 찾기 | 25분 | 25분 |
| 10분 | 휴식 | - | - |
| 50분 | **2교시**: 구조체 — 프로필 카드 만들기 | 20분 | 30분 |
| 10분 | 휴식 | - | - |
| 50분 | **3교시**: C 언어 총정리 & 종합 실습 | 15분 | 35분 |
| 10분 | Q&A 및 정리 | - | - |

> **강사 노트**: 포인터는 C 언어의 꽃입니다. "집 주소"라는 비유로 충분히 시간을 들여 설명하세요.

---

## 🎯 1교시: 포인터 기초 — "집 주소 찾기" 📮 (50분)

### 📖 이론 (25분)

#### 1.1 포인터란?

**포인터 = 집 주소를 적어놓은 메모지** 📮

```
변수 num = 42 가 메모리에 저장된 모습:

주소(집주소)   값(집 안의 물건)
───────────   ──────────────
  0x1000          42         ← 변수 num
  0x1004          ?
  0x1008          0x1000     ← 포인터 ptr (num의 주소를 저장!)
```

- **변수** = 값을 저장하는 공간 (집)
- **포인터** = 주소를 저장하는 변수 (집 주소 메모지)

#### 1.2 포인터 연산자: & 와 *

| 연산자 | 의미 | 비유 |
|--------|------|------|
| `&num` | num의 **주소**를 알려줘 | "이 집의 주소는?" |
| `*ptr` | ptr이 가리키는 **값**을 알려줘 | "이 주소에 가면 뭐가 있어?" |

```c
int num = 42;
int *ptr = &num;  // ptr에 num의 주소 저장

printf("num의 값: %d\n", num);      // 42
printf("num의 주소: %p\n", &num);    // 0x1000 (예시)
printf("ptr의 값: %p\n", ptr);       // 0x1000 (num의 주소)
printf("ptr이 가리키는 값: %d\n", *ptr); // 42
```

#### 1.3 포인터로 값 변경하기

```c
int num = 10;
int *ptr = &num;

*ptr = 99;  // ptr이 가리키는 곳의 값을 변경!

printf("%d\n", num);  // 99! (원본이 바뀜!)
```

#### 1.4 포인터와 함수 — Call by Reference

```c
// ❌ Call by Value (복사본 → 원본 안 바뀜)
void swap_fail(int a, int b) {
    int temp = a;
    a = b;
    b = temp;
}

// ✅ Call by Reference (주소 전달 → 원본이 바뀜!)
void swap(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

int main() {
    int x = 10, y = 20;
    
    swap_fail(x, y);
    printf("x=%d, y=%d\n", x, y);  // 여전히 10, 20 😢
    
    swap(&x, &y);  // 주소를 전달!
    printf("x=%d, y=%d\n", x, y);  // 20, 10 🎉
}
```

---

### 💻 실습 (25분)

#### 💻 실습 7-1: 포인터로 주소/값 출력 (5분)

```c
#include <stdio.h>

int main() {
    int a = 100;
    double b = 3.14;
    char c = 'A';
    
    int *pa = &a;
    double *pb = &b;
    char *pc = &c;
    
    printf("=== 변수와 포인터 ===\n");
    printf("a: 값=%d, 주소=%p\n", a, pa);
    printf("b: 값=%.2f, 주소=%p\n", b, pb);
    printf("c: 값=%c, 주소=%p\n", c, pc);
    
    // 포인터로 값 변경
    *pa = 200;
    *pb = 6.28;
    *pc = 'Z';
    
    printf("\n=== 포인터로 변경 후 ===\n");
    printf("a = %d\n", a);   // 200
    printf("b = %.2f\n", b); // 6.28
    printf("c = %c\n", c);   // Z
    
    return 0;
}
```

---

#### 💻 실습 7-2: 두 값 교환 (swap) (10분)

```c
#include <stdio.h>

void swap(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

int main() {
    int x = 10, y = 20;
    
    printf("교환 전: x=%d, y=%d\n", x, y);
    swap(&x, &y);
    printf("교환 후: x=%d, y=%d\n", x, y);
    
    return 0;
}
```

---

#### 💻 실습 7-3: 포인터로 배열 순회 (10분)

```c
#include <stdio.h>

void print_array(int *arr, int size) {
    for (int i = 0; i < size; i++) {
        printf("arr[%d] = %d (주소: %p)\n", i, *(arr + i), (arr + i));
    }
}

int main() {
    int scores[] = {85, 92, 78, 96, 88};
    
    printf("=== 포인터로 배열 순회 ===\n");
    print_array(scores, 5);
    
    // 포인터 증감
    int *ptr = scores;
    printf("\n첫 번째: %d\n", *ptr);     // 85
    ptr++;
    printf("두 번째: %d\n", *ptr);       // 92
    ptr += 2;
    printf("네 번째: %d\n", *ptr);       // 96
    
    return 0;
}
```

---

## 🎯 2교시: 구조체 — "프로필 카드 만들기" 🪪 (50분)

### 📖 이론 (20분)

#### 2.1 구조체란?

**구조체 = 프로필 카드** 🪪

```
┌──────────────────────┐
│  🐕 강아지 프로필     │
│  이름: 멍이           │
│  나이: 3              │
│  체중: 5.2 kg         │
└──────────────────────┘
```

각각의 정보(이름, 나이, 체중)의 타입이 다른데, 하나로 묶고 싶다!

```c
// 구조체 정의 (프로필 카드 양식)
struct Dog {
    char name[20];
    int age;
    double weight;
};
```

#### 2.2 구조체 사용

```c
#include <stdio.h>
#include <string.h>

struct Dog {
    char name[20];
    int age;
    double weight;
};

int main() {
    // 구조체 변수 생성 (프로필 카드 작성)
    struct Dog myDog;
    
    // 값 넣기
    strcpy(myDog.name, "멍이");  // 문자열은 strcpy!
    myDog.age = 3;
    myDog.weight = 5.2;
    
    // 출력
    printf("이름: %s\n", myDog.name);
    printf("나이: %d살\n", myDog.age);
    printf("체중: %.1f kg\n", myDog.weight);
    
    return 0;
}
```

#### 2.3 typedef로 편하게!

```c
// 매번 'struct Dog' 쓰기 귀찮다!
typedef struct {
    char name[20];
    int age;
    double weight;
} Dog;

// 이제 그냥 'Dog'으로 쓸 수 있음!
Dog myDog = {"멍이", 3, 5.2};
```

#### 2.4 구조체 포인터와 ->

```c
Dog myDog = {"멍이", 3, 5.2};
Dog *ptr = &myDog;

// 점(.)으로 접근
printf("%s\n", myDog.name);

// 화살표(->)로 접근 (포인터일 때!)
printf("%s\n", ptr->name);

// ptr->name == (*ptr).name 과 같음!
```

---

### 💻 실습 (30분)

#### 💻 실습 7-4: 강아지 프로필 (10분)

```c
#include <stdio.h>
#include <string.h>

typedef struct {
    char name[20];
    int age;
    double weight;
    char breed[20];  // 견종
} Dog;

void print_dog(Dog d) {
    printf("┌────────────────────┐\n");
    printf("│ 🐕 강아지 프로필    │\n");
    printf("├────────────────────┤\n");
    printf("│ 이름: %-12s │\n", d.name);
    printf("│ 견종: %-12s │\n", d.breed);
    printf("│ 나이: %d살          │\n", d.age);
    printf("│ 체중: %.1f kg       │\n", d.weight);
    printf("└────────────────────┘\n");
}

int main() {
    Dog dogs[3] = {
        {"멍이", 3, 5.2, "시바견"},
        {"초코", 2, 3.8, "푸들"},
        {"바둑이", 5, 12.0, "진돗개"}
    };
    
    for (int i = 0; i < 3; i++) {
        print_dog(dogs[i]);
        printf("\n");
    }
    
    return 0;
}
```

---

#### 💻 실습 7-5: 학생 구조체 배열 (20분)

```c
#include <stdio.h>
#include <string.h>

typedef struct {
    char name[20];
    int kor;
    int eng;
    int math;
    int total;
    double avg;
} Student;

void calc(Student *s) {
    s->total = s->kor + s->eng + s->math;
    s->avg = s->total / 3.0;
}

char get_grade(double avg) {
    if (avg >= 90) return 'A';
    if (avg >= 80) return 'B';
    if (avg >= 70) return 'C';
    if (avg >= 60) return 'D';
    return 'F';
}

int main() {
    Student students[3] = {
        {"홍길동", 95, 88, 92, 0, 0},
        {"김철수", 72, 85, 68, 0, 0},
        {"이영희", 88, 92, 95, 0, 0}
    };
    
    // 계산
    for (int i = 0; i < 3; i++) {
        calc(&students[i]);
    }
    
    // 출력
    printf("┌──────┬─────┬─────┬─────┬─────┬──────┬────┐\n");
    printf("│ 이름 │ 국어│ 영어│ 수학│ 총점│ 평균 │등급│\n");
    printf("├──────┼─────┼─────┼─────┼─────┼──────┼────┤\n");
    for (int i = 0; i < 3; i++) {
        Student s = students[i];
        printf("│%-6s│ %3d │ %3d │ %3d │ %3d │%5.1f │ %c  │\n",
               s.name, s.kor, s.eng, s.math,
               s.total, s.avg, get_grade(s.avg));
    }
    printf("└──────┴─────┴─────┴─────┴─────┴──────┴────┘\n");
    
    return 0;
}
```

---

## 🎯 3교시: C 언어 총정리 & 종합 실습 (50분)

### 📖 C 언어 핵심 총정리 (15분)

#### Day 01~07 복습 요약

```
┌─────────────────────────────────────────────────┐
│          🖥️  C 언어 핵심 로드맵                  │
│                                                 │
│  Day01  변수, 자료형, 입출력 (printf/scanf)      │
│    ↓                                            │
│  Day02  연산자, 조건문(if/switch)                │
│    ↓                                            │
│  Day04  반복문(for/while/do-while), break/continue│
│    ↓                                            │
│  Day05  함수, 매개변수, 반환값, static, 재귀      │
│    ↓                                            │
│  Day06  배열, 문자열, string.h                   │
│    ↓                                            │
│  Day07  포인터(&, *), 구조체(struct, typedef)     │
│                                                 │
│  🎉 이제 C++로 넘어갈 준비 완료!                  │
└─────────────────────────────────────────────────┘
```

| Day | 핵심 개념 | 한마디 정리 |
|-----|----------|------------|
| 01 | 변수, 입출력 | 데이터를 담고 보여주기 |
| 02 | 조건문 | 길을 선택하기 (if/switch) |
| 04 | 반복문 | 빙글빙글 반복하기 |
| 05 | 함수 | 코드를 레시피로 정리 |
| 06 | 배열, 문자열 | 사물함에 번호 붙이기 |
| 07 | 포인터, 구조체 | 주소와 프로필 카드 |

---

### 💻 종합 실습 7-6: 🎮 RPG 캐릭터 카드 게임 (35분)

**지금까지 배운 모든 것을 활용하는 종합 실습!**

```c
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>

// === 구조체 정의 ===
typedef struct {
    char name[20];
    char job[10];
    int hp;         // 체력
    int attack;     // 공격력
    int defense;    // 방어력
    int level;
    int exp;
} Character;

// === 함수 선언 ===
void create_character(Character *c);
void print_status(Character c);
void battle(Character *player, Character *monster);
int calc_damage(int attack, int defense);

int main() {
    srand(time(0));
    
    Character player;
    create_character(&player);
    
    printf("🎮 === RPG 모험 시작! === 🎮\n\n");
    print_status(player);
    
    // 몬스터 3마리와 전투
    char *monster_names[] = {"슬라임", "고블린", "드래곤"};
    int monster_hp[] = {30, 50, 100};
    int monster_atk[] = {5, 10, 20};
    int monster_def[] = {2, 5, 10};
    
    for (int i = 0; i < 3; i++) {
        Character monster;
        strcpy(monster.name, monster_names[i]);
        strcpy(monster.job, "몬스터");
        monster.hp = monster_hp[i];
        monster.attack = monster_atk[i];
        monster.defense = monster_def[i];
        monster.level = i + 1;
        
        printf("\n⚔️ %s 출현!\n", monster.name);
        battle(&player, &monster);
        
        if (player.hp <= 0) {
            printf("\n💀 게임 오버...\n");
            return 0;
        }
        
        // 승리 시 경험치 & 회복
        player.exp += 30;
        player.hp += 20;
        if (player.exp >= 100) {
            player.level++;
            player.exp -= 100;
            player.attack += 3;
            player.defense += 2;
            printf("🎉 레벨 업! Lv.%d\n", player.level);
        }
    }
    
    printf("\n🏆 축하합니다! 모든 몬스터를 물리쳤습니다!\n");
    print_status(player);
    
    return 0;
}

// === 함수 정의 ===
void create_character(Character *c) {
    printf("캐릭터 이름: ");
    scanf("%s", c->name);
    
    printf("직업 (1.전사 2.마법사 3.궁수): ");
    int choice;
    scanf("%d", &choice);
    
    switch (choice) {
        case 1:
            strcpy(c->job, "전사");
            c->hp = 100; c->attack = 15; c->defense = 10;
            break;
        case 2:
            strcpy(c->job, "마법사");
            c->hp = 70; c->attack = 20; c->defense = 5;
            break;
        default:
            strcpy(c->job, "궁수");
            c->hp = 85; c->attack = 18; c->defense = 7;
    }
    c->level = 1;
    c->exp = 0;
}

void print_status(Character c) {
    printf("┌─────────────────────┐\n");
    printf("│ 🪪 %s의 캐릭터 카드 │\n", c.name);
    printf("├─────────────────────┤\n");
    printf("│ 직업: %-8s Lv.%d │\n", c.job, c.level);
    printf("│ HP: %-3d ATK: %-3d    │\n", c.hp, c.attack);
    printf("│ DEF: %-3d EXP: %-3d   │\n", c.defense, c.exp);
    printf("└─────────────────────┘\n");
}

int calc_damage(int attack, int defense) {
    int dmg = attack - defense + (rand() % 5);
    return dmg > 0 ? dmg : 1;
}

void battle(Character *player, Character *monster) {
    while (player->hp > 0 && monster->hp > 0) {
        // 플레이어 공격
        int dmg = calc_damage(player->attack, monster->defense);
        monster->hp -= dmg;
        printf("  ⚔️ %s → %s에게 %d 데미지! (HP: %d)\n",
               player->name, monster->name, dmg,
               monster->hp > 0 ? monster->hp : 0);
        
        if (monster->hp <= 0) {
            printf("  🎉 %s를 물리쳤습니다!\n", monster->name);
            return;
        }
        
        // 몬스터 공격
        dmg = calc_damage(monster->attack, player->defense);
        player->hp -= dmg;
        printf("  💥 %s → %s에게 %d 데미지! (HP: %d)\n",
               monster->name, player->name, dmg,
               player->hp > 0 ? player->hp : 0);
    }
}
```

---

## 📝 과제

### 과제 1: 포인터로 배열 합계
배열의 합계를 구하는 함수를 포인터로 만드세요.
```c
int sum_array(int *arr, int size);  // 이 함수를 구현하세요
```

### 과제 2: 도서 구조체 관리
```c
typedef struct {
    char title[50];
    char author[30];
    int year;
    double price;
} Book;
```
- 3권의 도서 정보를 입력받아 배열에 저장
- 전체 도서 목록 출력
- 가장 비싼 도서 찾기

---

## 🎉 C 언어 파트 완료!

> **다음 시간 (Day 08)부터는 C++ 세계로 진입합니다!**
> C에서 배운 모든 것이 C++의 기초가 됩니다.
> 새로운 기능(클래스, string, 참조 등)이 추가되는 흥미진진한 여행이 시작됩니다! 🚀
